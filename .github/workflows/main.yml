name: RDP-Larger-Runner

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      # ... (các step cũ của bạn giữ nguyên: Display, Configure RDP, Create User, Connect Tailscale, Get IP, Verify)

      - name: Set Greenboy Wallpaper
        run: |
          $url = "https://img.pconline.com.cn/images/upload/upc/tx/wallpaper/1305/16/c2/21234567_1368776789333.jpg"
          $wallpaperPath = "$env:USERPROFILE\Pictures\greenboy-chill.jpg"
          
          Write-Host "Đang tải hình nền xanh chill..."
          Invoke-WebRequest -Uri $url -OutFile $wallpaperPath -TimeoutSec 300
          
          Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name WallpaperStyle -Value 10
          Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name TileWallpaper -Value 0
          
          Add-Type -TypeDefinition @"
          using System;
          using System.Runtime.InteropServices;
          public class Wallpaper {
              [DllImport("user32.dll", CharSet = CharSet.Auto)]
              public static extern int SystemParametersInfo(int uAction, int uParam, string lpvParam, int fuWinIni);
          }
"@ 
          [Wallpaper]::SystemParametersInfo(20, 0, $wallpaperPath, 3)
          
          Write-Host "Hình nền xanh chill đã gắn thành công! 💚☁️"

      - name: Maintain Connection
        run: |
          Write-Host "`n=== RDP ACCESS READY ==="
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          Write-Host "$env:RDP_CREDS"
          Write-Host "Wallpaper: Xanh chill cổ điển đã gắn"
          Write-Host "Specs: 4 vCPU | 16GB RAM | ~150GB free disk"
          Write-Host "Connect via Tailscale network with RDP client"
          Write-Host "========================================`n"
          
          while ($true) {
              Write-Host "[$(Get-Date)] Runner active - Cancel workflow to stop"
              Start-Sleep -Seconds 300
          }
