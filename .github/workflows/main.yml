name: MacOS VNC Ngrok Fix Màn Hình Đen

on: [push, workflow_dispatch]

jobs:
  macos-vnc:
    runs-on: macos-latest
    timeout-minutes: 360

    steps:
    - name: Download & Extract Ngrok
      run: |
        curl -s https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-darwin-amd64.zip -o ngrok.zip
        unzip ngrok.zip

    - name: Auth Ngrok
      run: ./ngrok authtoken $NGROK_AUTH_TOKEN
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}

    - name: Create & Setup VNC User (fix black screen)
      run: |
        sudo dscl . -delete /Users/vncuser || echo "User not exist"
        sudo dscl . -create /Users/vncuser
        sudo dscl . -create /Users/vncuser UserShell /bin/bash
        sudo dscl . -create /Users/vncuser RealName "VNC User"
        sudo dscl . -create /Users/vncuser UniqueID 1001
        sudo dscl . -create /Users/vncuser PrimaryGroupID 20
        sudo dscl . -create /Users/vncuser NFSHomeDirectory /Users/vncuser
        sudo cp -R /System/Library/User\ Template/English.lproj /Users/vncuser
        sudo chown -R vncuser:staff /Users/vncuser
        sudo dscl . -passwd /Users/vncuser mac12345
        sudo dscl . -append /Groups/admin GroupMembership vncuser

    - name: Enable VNC & Auto Login (fix black screen)
      run: |
        sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -access -on -privs -all -users vncuser
        sudo defaults write /Library/Preferences/com.apple.VNCSettings LocalPassword "mac12345"
        sudo defaults write /Library/Preferences/com.apple.loginwindow autoLoginUser vncuser
        sudo defaults write /Library/Preferences/com.apple.loginwindow autoLoginUserScreenLocked -bool false
        sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -restart -agent

    - name: Force Login Window (show desktop)
      run: |
        sudo launchctl load /System/Library/LaunchDaemons/com.apple.loginwindow.plist
        sleep 10  # chờ login xong

    - name: Show Connect Info
      run: |
        echo "=================================="
        echo "MACOS VNC FIX MÀN HÌNH ĐEN SẴN SÀNG!"
        echo "Username: vncuser (auto login)"
        echo "Password: mac12345 (nếu hỏi)"
        echo "Connect VNC bằng RealVNC Viewer hoặc TigerVNC"
        echo "Chờ ngrok hiện tcp://0.tcp.ngrok.io:xxxxx"
        echo "=================================="

    - name: Create Ngrok Tunnel Port 5900
      run: ./ngrok tcp 5900 --log=stdout

    - name: Keep Alive
      run: tail -f /dev/null
